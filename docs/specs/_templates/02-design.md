# 設計 — {{display_name}}

> status: 02-design.status を参照
> 前提: 01-requirements.status が `approved`

## 1. 設計方針

<!-- アーキテクチャ上の判断、既存パターンとの整合 -->

## 2. Laravel

### 2.1 Controller

| クラス | 新規/変更 | メソッド | 概要 |
|--------|----------|----------|------|
| | | | |

### 2.2 Service

<!--
一覧・参照系メソッドは、Eager Loading（`with()`）の対象リレーションとページネーション方針を
概要に含める（N+1 の予防。詳細: laravel-conventions skill「クエリ設計」）
-->

| クラス | 新規/変更 | メソッド | 概要 |
|--------|----------|----------|------|
| | | | |

### 2.3 Model / Enum

| クラス | 新規/変更 | 概要 |
|--------|----------|------|
| | | |

### 2.4 Migration

| ファイル名（案） | 操作 | 概要 |
|-----------------|------|------|
| | | |

#### インデックス

<!--
要件に根拠があるものは初期 Migration で張り切る（「重くなってから張る」にしない）。
FK は必ず（PostgreSQL は自動で張らない）、業務上の一意制約は VAL-xx を根拠に unique index、
検索・絞り込み・並び順は UC-xx / §5.2 を根拠に記載する。根拠 ID が書けないものは張らない。
複合インデックスの列順は「等値条件が先、範囲・ORDER BY が後」
（詳細: laravel-conventions skill「インデックス設計」）。張らないと判断した検索カラムが
ある場合は、その理由も行として残す（未検討と区別するため）。
-->

| テーブル | 対象列（複合は列順どおり） | 種類（index / unique / 部分） | 根拠（UC-xx / VAL-xx / §5.2 項目 / FK） |
|----------|---------------------------|------------------------------|----------------------------------------|
| | | | |

#### 削除方針・FK 削除時挙動

<!--
テーブルごとに論理削除 / 物理削除を設計時に決める。FK は削除時挙動
（cascade / restrict / set null）をデフォルト任せにせず明示する。
論理削除の場合は unique 制約との干渉（削除済みレコードと同値の再登録可否）を備考に書く
（詳細: laravel-conventions skill「削除設計」）。
-->

| テーブル | 削除方針（論理/物理） | 親テーブル（FK 先） | FK 削除時挙動 | 備考 |
|----------|----------------------|--------------------|---------------|------|
| | | | | |

### 2.5 View / Route

| View | Route | 概要 |
|------|-------|------|
| | | |

### 2.6 Job（該当時）

| クラス | 概要 |
|--------|------|
| | |

## 3. フロント（Blade / Vite / JavaScript）

### 3.1 Blade

| ファイル | 新規/変更 | 概要 |
|---------|----------|------|
| | | |

### 3.2 JavaScript（Vite エントリ）

| ファイル | 新規/変更 | 概要 |
|---------|----------|------|
| | | |

<!-- resources/js/ は JavaScript のみ。TypeScript は E2E（Playwright）専用 -->

## 4. シーケンス（主要フロー）

```
ユーザー → Blade → axios → Controller → Service → Model/DB
                              ↓
                         JSON レスポンス → DOM 更新
```

## 5. 非機能設計

### 5.1 トランザクション・整合性

<!--
1 トランザクションにまとめる範囲（検証と書き込みを同一トランザクションにするか）、
同時実行時に競合し得る処理（在庫チェック、重複チェック、採番等）とロック方針
（悲観ロック / ユニーク制約 / 許容）を記載する。競合が起きない設計なら理由を書く
-->

| 処理 | トランザクション範囲 | 競合の可能性 | 対策 |
|------|---------------------|-------------|------|
| | | | |

### 5.2 エラーハンドリング

<!-- Service が投げる例外と HTTP レスポンスの対応を記載する。要件定義 §7 と対応させる -->

| 事象 | 例外 / 検出箇所 | HTTP | レスポンス |
|------|----------------|------|-----------|
| | | | |

### 5.3 セキュリティ観点チェック

- [ ] 認可の担保箇所を明記した（どの層でロール・所有者判定をするか。フロントの表示制御だけに頼らない）
- [ ] mass assignment 対策（Model の `fillable` に必要最小限のみ列挙）
- [ ] XSS エスケープの担保箇所を明記した（Blade の `{{ }}` / JS で描画する場合はエスケープ関数）
- [ ] SQL インジェクション（生クエリ・`whereRaw` を使う場合はバインド必須。未使用なら「なし」）
- [ ] CSRF（フォーム・axios の CSRF トークン設定）
- [ ] ファイルアップロード（拡張子・MIME・サイズの検証、保存先は公開ディレクトリ外。
      アップロード機能がなければ「なし」）
- [ ] ログ・エラーレスポンスに機微情報を出さない（個人情報・スタックトレースを画面・
      レスポンスに出さない。担保箇所を明記）

## 6. 影響範囲

<!--
既存機能への影響、後方互換。共通コンポーネント・共通 Service・共通 Blade パーツ・
既存テーブルへのカラム追加等、今回の変更が波及しうる範囲を洗い出す。
ID はテスト計画（03-test-plan.md）の回帰確認表・CSV「出典」列から参照する（例: IMPACT-01）。
影響なしと判断した場合も、検討した上での判断であることが分かるよう理由を書く。
-->

| ID | 対象（既存の画面・API・共通部品・テーブル） | 変更内容 | 影響の可能性 | 回帰確認方針 |
|----|----------------------------------------------|----------|-------------|-------------|
| IMPACT-01 | | | | |

- [ ] 影響ありと判断した項目には、テスト計画（03-test-plan.md）に回帰確認のテストケースが対応している
- [ ] 影響なしと判断した項目は、その根拠（例: 新規テーブルのため既存データに触れない）が「回帰確認方針」に書かれている
