# テスト計画レビューチェックリスト（AI 独立レビュー）— アンケートシステム アカウント基盤（認証・企業/部署/管理者/ユーザー管理）

> 目的: `03-test-plan.md` §3 の「網羅基準」は**作成者（AI）のセルフチェック**であり、
> 作成者自身の見落としには気づけない。作成コンテキストを引き継がない**専用レビュー
> サブエージェント**（`sdd-plan-reviewer`、readonly）が、このチェックリストに沿って別視点で検証する。
>
> 運用: `.claude/rules/sdd-workflow.md`「フェーズ 3 > 独立レビュー（承認確認前に必須）」を参照。
> このレビューは人間の承認を代替しない（自己承認の禁止の原則は変わらない）。

## 使い方

1. `03-test-plan.md` と各 CSV の作成後、**承認確認を人間に提示する前**に、専用レビューサブエージェント
   `sdd-plan-reviewer`（定義: `.claude/agents/sdd-plan-reviewer.md`、readonly）へ
   `01-requirements.md` / `02-design.md` / `03-test-plan.md` / 各 CSV / 本チェックリストのパスを渡し、
   下記項目を ✓ / ✕ と理由で判定させる
2. ✕ の指摘のうち機械的に自動修正可能なもの（カバレッジ表の空欄、曖昧な期待結果の言い換え、
   出典 ID の記載漏れ、CSV 粒度の分割漏れ、**ピラミッド違反** 等）はメインの AI がその場で修正する
3. 業務ルールの解釈・仕様追加の要否など判断が必要な指摘は自動修正せず、承認確認の提示に
   含めて人間に判断を仰ぐ
4. 判定結果と対応状況を「6. 総合判定」の実施記録に記入する

## 0. テストピラミッド（レイヤ分担）

正本: `.claude/skills/testing-pyramid/SKILL.md`

- [x] `03-test-plan.md` §1（テストピラミッド）が記載されているか
- [x] 複合 VAL 分解表（§1.3）があり、サブルールごとに Test ID または E2E-only 理由があるか
- [x] 閾値チェック一覧（§1.4）があり、NG/OK ペアが PHPUnit または Vitest にあるか
- [x] E2E の `inp` が **1 画面 1 スモーク**＋クリティカル異常に留まっているか（項目ごとの inp 量産がないか）
- [x] FormRequest / HTTP 422 の VAL が PHPUnit（`layer:unit` / `layer:http`）に計画されているか
- [x] `02-design.md` §6 の Service 検証分岐が PHPUnit に 1:1 で計画されているか
      （初回レビューで✕: 更新系〔Admin/User/DepartmentUpdateRequest〕の必須項目空入力ケースが
      Store 系 Test ID の流用になっていた。PU-127〜134 を追加し解消）
- [x] §1.3 / §1.4 の表が正本どおりの列定義になっているか
- [x] §1.5（E2E 棚卸し）があり、**維持** と判定した Test ID のみが `03-test-plan.csv` に存在するか
- [x] 各 E2E がメインフロー / クリティカル異常 / 画面スモークのいずれかであり、§1.5 に理由があるか
- [x] 下層と同観点の E2E 重複がないか（件数比が高くても、重複がなければ削減不要）
- [x] E2E `auth` に HTTP 403/404 のみを確認するケースが残っていないか（PHPUnit HTTP へ移行すべきか）
- [x] 絞り込み・集計の条件分岐が **1 条件 1 E2E** になっていないか（代表 1 件 + PHPUnit 網羅か）
- [x] 類似ジャーニー（ロール違い登録・ナビ単体・重複 dsp）が統合または削減されているか

## 1. トレーサビリティ

- [x] 要件カバレッジ表に UC / VAL / AC / NFR / 画面項目 / 権限の全項目が記載されているか
- [x] カバレッジ表の「対応 Test ID」が空欄の行がないか
- [x] NFR-xx が「対応不可」の場合、理由と代替手段が明記されているか（該当なし）
- [x] `IMPACT-xx` に回帰確認の対応 Test ID が割り当てられているか
- [x] CSV「出典」列の要件 ID が `01-requirements.md` に実在するか
      （初回レビューで✕: PU-028 の出典が `Q-15` 単独で規約が想定する ID 種別に該当しなかった。
      `UC-08;Q-15` に修正して解消）
- [x] 逆方向（Test ID → 要件 ID）にも意味不明な出典がないか

## 2. 期待結果の質

- [x] 期待結果が具体的な値・文言・件数・画面状態で書かれているか
- [x] エラーケースでエラーメッセージの文言まで固定されているか
      （初回レビューで✕: PU-095/096 の「(UTF-8)」が半角括弧で `01-requirements.md` §7 の確定文言
      〔全角括弧〕と不一致、PU-097/099 の「1000件」がカンマなしで確定文言〔1,000件〕と不一致。
      いずれも確定文言どおりに修正して解消）
- [x] 同じ観点のケース間で期待結果の記述粒度が大きくブレていないか

## 3. ケース粒度・独立性

- [x] 1 行 1 観点になっているか
- [x] 前提条件が単独で再現可能な記載か
- [x] 複数ケースが同一データを書き換える場合、干渉しないようテストデータが分離されているか
- [x] 実行順序への依存がないか
- [x] 並列実行安全性

## 4. 網羅性

- [x] 正常系・異常系が対になっているか
- [x] 境界値の両側（ちょうど / 超過）が確認されているか
- [x] 全ロール × 全操作の権限パターンが確認されているか（PHPUnit HTTP で網羅）
      （初回レビューで✕: ユーザー CSV 登録の role=user 403 確認が欠落していた。PU-135 を追加して解消）
- [x] 要件で許可されている操作・状態遷移が全レイヤ合算で列挙されているか
- [x] 更新系がある場合、必須項目・任意項目それぞれの空入力ケースが全項目分あるか
      （初回レビューで✕: 指摘①と同一。PU-127〜134 の追加で解消。加えて CSV 行内「入社年月日」
      形式不正ケース〔VAL-08〕が欠落していたため PU-136 を追加）

## 5. 実装可能性（先読みリスク）

- [x] Playwright で実際に自動化できる内容か
- [x] PHPUnit / Vitest で担保すると書かれている項目が設計書の該当クラス・関数と対応しているか
- [x] 固定時間の待機に頼った書き方を前提にしていないか

## 6. 総合判定

| 状態 | 条件 | 対応 |
|------|------|------|
| 指摘なし | **0〜5** のすべての項目が ✓ | そのまま承認確認を人間に提示する |
| 自動修正済み | ✕ はあったが機械的に修正できた | 修正内容を記載の上、承認確認を人間に提示する |
| 要判断 | 業務ルールの解釈等、自動修正できない ✕ が残る | 承認確認に残課題として明記し、人間に判断を仰ぐ |

**判定: 自動修正済み**（要判断の指摘は 0 件）

初回レビューで指摘された 6 件はすべて機械的に修正可能であり、以下のとおり対応済み。

1. 更新系（Admin/User/Department の UpdateRequest）必須項目空入力ケースの欠落
   → PU-127〜134 を追加し、`03-test-plan.md` §1.3・§3「更新系の標準観点」・要件カバレッジ表を更新
2. エラーメッセージ文言の確定仕様との不一致（VAL-12 括弧・VAL-13 カンマ）
   → PU-095, PU-096, PU-097, PU-099 の期待結果を確定文言どおりに修正
3. ユーザー CSV 登録の role=user 403 確認の欠落
   → PU-135 を追加し、要件カバレッジ表「ユーザー CSV 登録（§8）」行に反映
4. CSV「出典」列の ID 種別不一致（PU-028 の `Q-15` 単独表記）
   → `UC-08;Q-15` に修正
5. CSV 行内「入社年月日」形式不正ケースの欠落（VAL-08）
   → PU-136 を追加し、§1.3 VAL-08 行に反映
6. §2 カテゴリ別件数表の実カウント不一致
   → CSV の実カウント（E2E: inp4/auth3/evt8/dsp1/trn1、PHPUnit: inp68/auth30/evt21/dsp4/other13、
   合計 E2E17・PHPUnit136）に基づき表を修正

修正後、`npm run lint:sdd -- survey-accounts` を再実行し ERROR 0 / WARN 0 を確認済み。

<!-- レビュー実施記録 -->

| 実施日 | レビュー方式 | 判定 | 指摘事項と対応 |
|--------|-------------|------|----------------|
| 2026-07-13 | 独立サブエージェント（`sdd-plan-reviewer`） | 自動修正済み | 上記 6 件すべて機械修正済み。要判断の残課題 0 件 |
