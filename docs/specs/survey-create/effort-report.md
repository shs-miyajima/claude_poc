# 工数レポート — アンケート作成（設問作成・編集・削除・下書き/公開・代理作成）

> このファイルは SDD の各フェーズで人手想定工数と実績時間を記録し、
> 工数削減効果を確認するためのもの。承認ゲートの対象外（status ファイルなし）。
> 全体（4 スペック合算）の配分は `../survey-system/effort-report.md` を参照。

## 1. 人手想定工数（見積）

**フェーズ 1 開始時に記入する。** 「経験のある中堅エンジニア 1 名が、AI を使わず
手作業で同じ成果物を作成した場合」の工数を見積もる。

### 見積の前提

- 想定スキル: Laravel 経験 3 年以上の中堅エンジニア 1 名
- 含むもの: 成果物の作成・自己レビュー・手戻り 1 回分
- 含まないもの: 承認者のレビュー時間、環境構築、仕様の原典作成

### フェーズ別見積

| フェーズ | 主な成果物 | 見積根拠（規模） | 人手想定工数 |
|---------|-----------|----------------|-------------|
| 1. 仕様整理 | 01-requirements.md, open-questions.md | UC 13 件・画面 4 面・VAL 14 件・AC 10 件・不明点 11 件（親からの引き継ぎ 2 件を含む）の質疑と反映 | 5h |
| 2. 設計 | 02-design.md | 新設テーブル 3（surveys/questions/choices）・設問形式 4 種と選択肢の動的組み立て UI の方式設計・Controller/Service 約 8〜10 クラス・画面 4 面 | 6h |
| 3. テスト設計 | 03-test-plan.md + CSV | PHPUnit 約 60 件（設問形式ごとの検証・下書き/公開の状態遷移・テナント分離・境界値）・E2E 約 8 件（作成〜公開ジャーニー + クリティカル異常）・カバレッジ表 | 6h |
| 4. 実装・テスト | コード・テストコード | 新規約 25 ファイル（Migration 3・Model 3・Service/Controller・FormRequest・Blade 4 画面 + 設問/選択肢の動的組み立て JS）+ テスト約 68 件 | 16h |
| **合計** | | | **33h（約 4.1 人日）** |

※ 1 人日 = 8h で換算する。
※ 見積は survey-accounts（先行スペック）の実績規模（新規約 30 ファイル・PHPUnit 136 件・E2E 17 件・40h 見積）を
参考に、本機能の規模（画面数少・ただし設問/選択肢のネスト構造で複雑度は同程度）に合わせて算出した。
規模の前提が大きく変わった場合は見積を更新し、変更理由をここに残す。

## 2. 実績記録

**各フェーズの承認時（フェーズ 4 は完了時）に記入する。** 経過時間はフェーズ着手から
承認（完了）までのチャット時刻を基準とし、人間の確認・承認待ちを含む。

| フェーズ | 着手 | 承認/完了 | 経過時間 | 差戻し回数 | 備考 |
|---------|------|----------|---------|-----------|------|
| 1. 仕様整理 | 2026-07-14 11:04 | 2026-07-14 11:31 | 27m | 1 回 | open-questions 11 件（親からの引き継ぎ 2 件含む）の質疑応答 → 独立レビュー（sdd-requirements-reviewer）で機械修正 6 件を検出・解消 → 承認確認提示後、ユーザー指摘（§5.2 の UI コントロール未指定）により差戻し 1 回 → 修正 → 承認 |
| 2. 設計 | | | | | 実績時間が未記録（承認は `changelog.md` 2026-07-14 分・02-design.status を参照。差戻し 1 回〔選択肢2件時の削除ボタン挙動〕を含む） |
| 3. テスト設計 | | 2026-07-14 14:13 | | 0 回 | 着手時刻が未記録（承認は 03-test-plan.status を参照） |
| 4. 実装・テスト | 2026-07-14 14:13 | 2026-07-14 15:50 | 1h37m | 0 回 | PHPUnit 88 件（計画86＋回帰確認2）・Vitest 15 件・E2E 7 件を実装し全件成功。実装中に設問並び替え時のラジオボタン選択解除バグ（§6 参照）を検出・修正。完了報告後のユーザーレビューで3件の追加指摘（設問形式初期値・エラー表示・設問入力保持不具合）を受け、01-requirements.md（差分承認）・03-test-plan-vitest.csv（差分承認・VT-015追加）を含めて対応 |
| **合計** | | | **2h4m（フェーズ1・4のみ。フェーズ2・3は実績時間未記録のため参考値）** | 2 回（フェーズ1・2 各1回。フェーズ4内での差分承認2回〔reopened〕は仕様の手戻りではなく完了後レビューによる追加のため差戻し回数に含めない） | |

## 3. 削減効果

**フェーズ 4 完了時に算出する。**

| 指標 | 値 |
|------|-----|
| 人手想定工数（合計） | 33h |
| 実績時間（合計・参考値） | 2h4m（フェーズ1・4のみ。フェーズ2・3の実績時間が未記録のため全体の実績合計は算出不可） |
| 削減時間（参考値） | フェーズ1・4だけでも 30h56m の削減（33h − 2h4m） |
| 削減率（参考値） | 約94%（フェーズ2・3の実績時間が不明なため保守的な値ではない点に注意） |

### 所感（任意）

- フェーズ2・3の実績時間（§2）が記録されておらず、全体の削減効果を厳密には算出できない。
  今後は各フェーズ承認時に着手・承認時刻を都度記入する運用を徹底する
- フェーズ4（実装・テスト）はPHPUnit 86件・Vitest 15件・E2E 7件・新規ファイル約25件の実装に加え、
  完了報告後のユーザーレビュー指摘2件（設問形式初期値・エラー表示）への対応を含めて1h24mで
  完了しており、人手想定16hに対して大幅な削減となった
