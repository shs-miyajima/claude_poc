# 未解決の質問 — survey-create

<!-- フェーズ 1 でユーザーへの質問を記載。解決したら回答を追記し、01-requirements.md に反映 -->

システム全体の Q&A は `../survey-system/open-questions.md`（Q1〜Q41）が正本。
ここでは survey-create 固有の詰め切り事項（親 Q39・Q40 の引き継ぎを含む）を管理する
（番号は本ファイル内で独立）。

- 起票日: 2026-07-14
- 回答日: 2026-07-14
- 全 11 件 resolved（open 0 件）

| # | 質問 | 回答 | 状態 |
|---|------|------|------|
| 1 | 【親 Q39 の引き継ぎ・自由記述設問の回答文字数上限】自由記述設問に対する回答の文字数上限はいくつにしますか。親 open-questions では未回答（推奨: 2,000 文字）。本機能（設問定義）の VAL・データ要件に直結するため、survey-create のフェーズ 1 で確定します | 2,000 文字（推奨案どおり） | resolved |
| 2 | 【親 Q40 の引き継ぎ・段階評価の表示ラベル方式】段階評価（1〜5 固定）の「どちらが良い/悪いか」の提示方法をどうしますか。（a）数値のみ表示し、意味は設問文に書く運用（b）作成時に両端のラベルを任意入力できる（例: 1=全くそう思わない / 5=とてもそう思う。未入力なら数値のみ表示）（c）固定ラベル（1=全くそう思わない〜5=とてもそう思う）を常に表示。推奨: (b)（回答者が方向を誤読しにくく、設問ごとに意味を変えられるため）。(b) の場合、作成画面に両端ラベルの入力欄（各 500 文字以内、任意入力）が必要になります | (b) 両端ラベルを任意入力可（推奨案どおり）。未入力なら数値のみ表示 | resolved |
| 3 | 【設問の必須/任意のデフォルト値】親 Q17 で「設問ごとに必須/任意を設定可能」は確定していますが、新規追加時の初期値は未確定です。デフォルトを「必須」としてよいですか。推奨: はい（未設定のまま作成者が気づかず任意になるより、意図的に外す運用の方が安全なため） | 任意をデフォルトにする（推奨と異なる。作成者が必要な設問のみ意図的に必須へ切り替える運用とする） | resolved |
| 4 | 【選択肢数の下限】単一選択・複数選択の選択肢数は上限 10（親 Q14 で確定）ですが、下限は未確定です。最低 2 件必要としますか。推奨: はい（選択肢が 1 件以下では「選択」の意味がなくなるため） | 2 件以上必須（推奨案どおり） | resolved |
| 5 | 【設問の並び順変更】作成画面で設問の並び順を後から変更（例: ドラッグ&ドロップや上下ボタン）できる必要がありますか。それとも追加した順序で固定でよいですか。推奨: 変更可能にする（作成中に順序を調整するニーズが高いため） | 変更可能にする（推奨案どおり。UI 方式は 02-design で確定） | resolved |
| 6 | 【削除方式（論理/物理）】親の全体方針（Q32）では企業・アカウントの削除は論理削除ですが、下書き状態のアンケート・設問・選択肢（回答実績が存在しない）の削除も同じ方針を適用しますか。それとも実績のないデータのため物理削除としますか。推奨: 物理削除（下書きは公開前で回答実績がなく、論理削除で一覧に残す意味がないため。公開後は削除不可のため物理削除の対象は常に無回答データ） | 物理削除（推奨案どおり） | resolved |
| 7 | 【代理作成時の作成者表示】スーパーユーザーが個別企業画面に切り替えてアンケートを代理作成した場合、一覧・詳細画面の「作成者」表示はどうしますか。（a）スーパーユーザー名を表示（b）作成者情報自体を表示しない（企業名のみ）。推奨: (a)（代理作成の事実が管理者側にも分かるようにするため） | (b) 表示しない（推奨と異なる。作成者情報自体を画面に出さない） | resolved |
| 8 | 【一覧のページング】アンケート一覧は survey-accounts の他一覧（企業・部署・管理者・ユーザー）と同様に 1 ページ 20 件のページングでよいですか。推奨: はい（既存画面との一貫性のため） | 20 件/ページ（推奨案どおり） | resolved |
| 9 | 【タイトルの重複可否】同一企業内で同じアンケートタイトルを複数登録できますか。推奨: 可能（一意制約なし。複数回同じテーマで実施するケースがあるため） | 可能（推奨案どおり。一意制約なし） | resolved |
| 10 | 【複数選択の選択数制約】複数選択設問で、選択肢のうち何個まで選べるか（最小/最大選択数）を作成者が設定できる必要がありますか。それとも常に「1 個以上・上限なし（用意した選択肢全部を選択可）」でよいですか。推奨: 制約設定は不要（常に 1 個以上・上限なし。必要になれば追加検討） | 不要（推奨案どおり。常に 1 個以上・上限なし） | resolved |
| 11 | 【回答数・回答率の表示範囲】アンケート一覧・詳細画面（本機能のスコープ）で回答数・回答率を表示しますか。それともダッシュボード（survey-dashboard, ④）でのみ表示し、本機能では表示しませんか。推奨: 本機能では表示しない（回答率表示は親決定〔Q25〕でダッシュボードの機能と定義されているため。本機能の詳細画面は設問構成の確認のみ） | 表示しない（推奨案どおり） | resolved |
